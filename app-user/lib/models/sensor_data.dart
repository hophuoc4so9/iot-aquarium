/// Model demo cho dữ liệu sensor (KHÔNG dùng cho API thực tế)
/// Chỉ dùng cho UI demo
class SensorData {
  final double waterLevel;
  final double temperature;

  SensorData({required this.waterLevel, required this.temperature});
}
